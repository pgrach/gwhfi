name: Telemetry Recovery Notice

on:
  # Railway is the authoritative continuous telemetry writer. This manual-only
  # workflow intentionally performs no write; it is retained temporarily so a
  # queued legacy invocation cannot create a duplicate observation.
  workflow_dispatch:

jobs:
  run-worker:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Explain authoritative collector
      run: echo "Telemetry ingestion runs continuously on Railway; no GitHub write was attempted."
